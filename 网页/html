<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>示例网页</title>
    <style>
      :root {
        --brand: #0366d6;
        --bg: #f7f7f7;
        --card: #fff;
      }
      body {
        font-family: Segoe UI, Roboto, Helvetica, Arial, sans-serif;
        margin: 0;
        background: var(--bg);
        color: #222;
      }
      header,
      footer {
        background: var(--brand);
        color: #fff;
        padding: 12px 16px;
      }
      main {
        max-width: 900px;
        margin: 24px auto;
        padding: 16px;
        background: var(--card);
        border-radius: 6px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
      }
      nav a {
        color: #fff;
        margin-right: 12px;
        text-decoration: none;
      }
      .btn {
        display: inline-block;
        background: var(--brand);
        color: #fff;
        padding: 8px 12px;
        border-radius: 4px;
        text-decoration: none;
      }
      .zuobian {
        right: 190px;
      }
    </style>
  </head>
  <body>
    <header>
      <h1 style="margin: 0; font-size: 1.25rem">示例网页</h1>
      <nav aria-label="主导航" style="margin-top: 8px">
        <a href="#" aria-current="page">主页</a>
        <a href="#" class="zuobian">关于</a>
        <a href="#">联系</a>
      </nav>
    </header>

    <main>
      <h2>欢迎</h2>
      <p>
        这是一个简洁的 HTML5 模板。将此内容保存为 .html
        文件，然后在浏览器中打开以查看效果。
      </p>
      <p><a class="btn" href="#">开始使用</a></p>
    </main>

    <footer>
      <small>© 2025 示例公司</small>
      <a href="#" class="zuobian">关于</a>
      <a href="#" class="zuobian">联系</a>
    </footer>
  </body>
</html>
